workflows:
  web-build:
    name: Web Build
    max_build_duration: 30
    environment:
      node: latest
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build web app
        script: |
          npm run build
    artifacts:
      - build/**
      - dist/**